# docker-compose.yml

version: '3.7'

services:
    database:
        env_file:
            - ./docker/database/.env.test

    application:
        env_file:
            - ./docker/application/.env.test
            - ./docker/database/.env.test